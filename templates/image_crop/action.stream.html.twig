<turbo-stream action="replace" target="cropper">
    <template>
        <div class="card">
            <div class="card-header">
                Crop Image
            </div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </template>
</turbo-stream>